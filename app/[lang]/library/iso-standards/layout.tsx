import { Metadata } from "next";

export const metadata: Metadata = {
  title: "ISO Compliance Software Pricing | Plans & Features",
  description: "Explore ISOBrain monthly subscription plans. Get access to ISO compliance tools, AI audit features, mastery labs, and Academy training resources. |",
};

export default function ISOStandardsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
