import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Quality Management Courses | ISO 9001 Training",
  description: "Learn quality management systems with ISO 9001-focused courses. Build practical skills using structured lessons and AI-supported tools.",
};

export default function QualityManagementLayout({ children }: { children: React.ReactNode }) {
  return children;
}
