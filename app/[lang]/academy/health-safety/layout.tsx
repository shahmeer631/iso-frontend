import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Health & Safety Courses | ISO 45001 Training",
  description: "Build workplace safety expertise with ISO 45001 courses. Learn how to manage risk, ensure compliance, and improve safety systems.",
};

export default function HealthSafetyLayout({ children }: { children: React.ReactNode }) {
  return children;
}
