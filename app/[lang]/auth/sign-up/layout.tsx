import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Account | ISOBrain Compliance Software",
  description: "Create your ISOBrain account and start managing ISO compliance with AI-powered tools and structured workflows.",
};

export default function SignUpLayout({ children }: { children: React.ReactNode }) {
  return children;
}
